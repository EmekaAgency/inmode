import React from "react";
import { MenusContext_Interface } from "../interfaces";

export const MenusContext:React.Context<MenusContext_Interface> = React.createContext({
    
});

export default MenusContext;