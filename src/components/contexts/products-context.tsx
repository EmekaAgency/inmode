import React from "react";
import { ProductsContext_Interface } from "../interfaces";

export const ProductsContext:React.Context<ProductsContext_Interface> = React.createContext({
    
});

export default ProductsContext;